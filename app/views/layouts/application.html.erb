<!DOCTYPE html>
<html>
  <head>
    <title>ScagsBourse</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1> <a href="/">Bourse d'échange Ski Club Alpe du Grand Serre</a> </h1>
      <ul>
        <% if user_signed_in? %>
          <li> <%= current_user %> </li>
          <li> <%= button_to('Déconnexion', destroy_user_session_path, method: :delete) %></li>
        <% else %>
          <li> <%= button_to('Connexion', new_user_session_path, method: :get) %> </li>
          <li> <%= button_to('Inscription', new_user_registration_path, method: :get) %> </li>
        <% end %>
      </ul>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
